# environment.yml
name: hl7_fhir_env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      # Install the project itself in editable mode
      - -e .

      # Core RDF/SHACL dependencies
      - rdflib>=7.0.0
      - pyshacl>=0.25.0
      
      # Development/test dependencies
      - pytest>=8.2
      - pytest-cov>=5.0
      - mypy>=1.11
      - ruff>=0.6.0
      - types-PyYAML
