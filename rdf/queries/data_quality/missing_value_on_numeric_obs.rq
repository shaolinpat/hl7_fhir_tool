PREFIX hft:  <https://w3id.org/shaolinpat/hft#>

SELECT ?observation WHERE {
    ?observation a hft:Observation ;
                 hft:hasUnit ?u . 
    FILTER (!EXISTS { ?observation hft:valueDecimal ?v } && !EXISTS { ?observation hft:valueInteger ?i } )
}
